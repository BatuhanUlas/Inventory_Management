const config = {
  db: {
    connectionLimit: 100,
    host: "localhost",
    user: "root",
    password: "",
    database: "employeeDeviceManagement",
  },
  listPerPage: 20,
};

module.exports = config;
